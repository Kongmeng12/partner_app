<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only.

  Android 9 and later refuse plaintext HTTP by default, and the refusal looks
  like an ordinary connection failure — which is a long way to travel to
  discover that the development API at http://10.0.2.2:3100 was never reachable.
  Cleartext is therefore allowed for exactly two hosts and nothing else.

  10.0.2.2 is how the Android emulator reaches the host machine; localhost
  inside the emulator means the emulator itself.

  Release builds do not include this file, so production stays TLS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
